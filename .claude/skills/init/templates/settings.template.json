{
  "_comment": "Generated by ai-native-blueprint. Baseline permissions extended with install-interview answers. Team-owned after install.",
  "_syntax": "Rules are Tool(pattern) with repo-relative glob patterns, e.g. Write(config/**) or Read(**/*secret*). One rule per array entry — no free text.",
  "permissions": {
    "deny": [
      "Read(**/.env*)",
      "Read(**/*secret*)",
      "Read(**/*credential*)",
      "Write(**/.env*)"
    ],
    "ask": [
      "{{ask_first_paths_from_interview}}"
    ]
  }
}
