# {{project_name}}

<!-- Generated by ai-native-blueprint v{{blueprint_version}} on {{install_date}}. Team-owned after install. -->
<!-- Quality rules: keep under ~150 lines total; every command below must be copy-paste runnable. -->

{{one_paragraph_project_description}}

## Stack

{{stack_with_versions}}

## Commands

<!-- Every command verified at generation time. If one stops working, fix it here immediately — a wrong command misleads every agent session. -->
<!-- A command that doesn't exist for this repo (no build step, no lint) is OMITTED and replaced with a one-line note (e.g. "# no build step — plain JS") — never invent one. -->
<!-- Flag commands that fetch a tool over the network on first use, e.g. "# npx fetches prettier (not a devDependency)". -->

```bash
{{bootstrap_command}}        # one-command working environment
{{build_command}}
{{test_all_command}}
{{test_file_command}}        # file-scoped: seconds, not minutes
{{lint_file_command}}        # file-scoped
```

## Structure

{{directory_map_with_one_line_per_area}}

## Code example

<!-- One real example from this repo showing the house style. Replaces style prose. -->
<!-- Quote it VERBATIM — no `// ...` elisions. An abridged snippet that differs from the real code misleads agents about actual behavior. Prefer a short function quoted whole. -->

```{{language}}
{{representative_code_snippet}}
```

## Tests

- Runner: {{test_runner}}
- Test file naming: `{{test_file_pattern}}`
- Exemplary test file (copy its conventions): `{{exemplary_test_file}}`

## Boundaries

**Always:** {{always_list}}

**Ask first:** {{ask_first_list}}

**Never:** {{never_list}}

## Team tools

- Tracker: {{tracker_name}} — `{{tracker_view_command}}`, `{{tracker_create_subtask_command}}`
- VCS: {{vcs_host}} — `{{pr_create_command}}`, `{{pr_view_command}}`
- Auth: {{auth_model}}

## Git workflow

- Branches: `{{branch_naming}}`; never push to `{{default_branch}}` directly
- Worktrees: one per task in `{{worktree_location}}`, one agent per worktree, cleanup after merge (see parallel-work skill). The already-checked-out branch counts as the current task's worktree — create new worktrees only for additional parallel tasks

## Conventions

{{unusual_conventions_only}}
